# TRANSITIONAL COMPATIBILITY SHIM.
#
# The September 11 source registry is now owned by its collection and lives at
# src/evidence_collections/september11/sources.yaml. This file stays so that
# existing CLI invocations, CI workflows and scripts that pass
# `config/sources.phase0.yaml` keep working unchanged.
#
# `sources_from` is a generic registry-composition feature of
# historical_engine.sources; paths resolve relative to this file.
sources_from:
  - ../src/evidence_collections/september11/sources.yaml
